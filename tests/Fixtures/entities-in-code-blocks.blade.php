```php
$this->assertStringContainsString('&lt;script&gt;', $content);
```

Run {{ 'php scribe tinker --execute "User::count()"' }} to check.

```bash
php scribe tinker --execute {{ '"$a < $b && $c"' }}
```
