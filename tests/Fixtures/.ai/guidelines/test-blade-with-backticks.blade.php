# Blade Template with Backticks

Use `scribe make:model` to create models.
Run `php scribe migrate` for migrations.

Code example with backticks:
- `Model::query()`
- `route('home')`
- `config('app.name')`
